angular.module('ngApp.buildConsignmment').controller('BuildConsignmentController', function ($scope, $state, Upload, config, $location, $filter, $translate, CustomerService, SessionService, $uibModal, uiGridConstants, toaster, $window) {


   

    function init() {
       
    }

   init();

});