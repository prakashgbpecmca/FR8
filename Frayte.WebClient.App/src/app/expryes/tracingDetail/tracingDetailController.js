angular.module('ngApp.buildConsignmment').controller('TracingDetailController', function ($scope, $state, Upload, config, $location, $filter, $translate, CustomerService, SessionService, $uibModal, uiGridConstants, toaster, $window) {




    function init() {

    }

    init();

});